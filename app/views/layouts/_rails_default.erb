<!-- Protect against cross-site request forgery -->
<%= csrf_meta_tags %>

<!-- Call to a function to include stylesheets with two args:
     string with stylesheet path & hash with 2 elements
     Parentheses on function calls are optional
     When hashes are the last argument in a function call, the curly braces are optional
     Ruby doesn't distinguish between newlines and other whitespace -->
<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
<%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
